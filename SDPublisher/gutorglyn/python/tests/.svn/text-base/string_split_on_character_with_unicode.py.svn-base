
searchStr = '128b - Moliant i Syr Rhisiart Gethin'
number = searchStr.split('-')[0]

#remove all white space at the end
number = number.strip()

print 'At the start the number is ' + str(number)

numLength = len(number)

print 'At the start the length is ' + str(number)

if numLength == 1:
    number =  '00' + number

elif numLength == 2:
    number =  '0' + number

elif numLength >= 3:
    number = number

print 'At the end the number is ' + str(number) + ' and the length is ' + str(numLength)