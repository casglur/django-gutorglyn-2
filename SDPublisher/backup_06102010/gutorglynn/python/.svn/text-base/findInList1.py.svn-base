def fndZero(list):
    iter, count = 0, 0
    while iter < len(list):
        if not list[iter]:
            print "Zero found at position %d in the list"
            count = count + 1
        iter += 1
    return count

test = [0, 0, 1, 2, 3, 0, 3, 2, 1, 0, 0, 0]
print "There are %d zeros in the test list" % fndZero(test)
